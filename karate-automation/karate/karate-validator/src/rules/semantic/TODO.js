/**
 * Placeholder cho Semantic Rules (KRTV-E001 đến KRTV-E006).
 * TODO: Implement các rule sau:
 *
 * KRTV-E001 — method-before-url
 *   url/path phải được khai báo trước khi gọi method.
 *
 * KRTV-E002 — invalid-http-method
 *   method chỉ nhận: get | post | put | delete | patch | head | options.
 *
 * KRTV-E003 — invalid-match-marker
 *   match marker phải đúng chính tả: #string #number #boolean #array #object
 *   #null #notnull #present #notpresent #ignore #uuid #date #regex.
 *
 * KRTV-E004 — invalid-status-code
 *   status phải là số nguyên 3 chữ số trong khoảng 100-599.
 *
 * KRTV-E005 — call-file-not-found
 *   call / callonce phải trỏ đến file tồn tại hoặc classpath hợp lệ.
 *
 * KRTV-E006 — undefined-variable
 *   Biến được dùng (#(varName) hoặc <varName>) phải được khai báo trước đó.
 */
