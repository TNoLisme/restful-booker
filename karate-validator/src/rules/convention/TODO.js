/**
 * Placeholder cho Convention Rules (KRTV-C001 đến KRTV-C005).
 * TODO: Implement các rule sau:
 *
 * KRTV-C001 — missing-scenario-tag
 *   Mỗi Scenario phải có ít nhất 1 tag.
 *
 * KRTV-C002 — duplicate-scenario-name
 *   Tên Scenario không được trùng nhau trong cùng 1 file.
 *
 * KRTV-C003 — too-many-steps
 *   Không quá N steps/scenario (mặc định 20, cấu hình được).
 *
 * KRTV-C004 — background-too-long
 *   Background không nên quá N steps (mặc định 5).
 *
 * KRTV-C005 — hardcoded-url
 *   Không dùng hardcode URL — phải dùng biến (configurable whitelist).
 */
